export default function evaluate (
  leftDataType,
  leftValue,
  rightDataType,
  rightValue
) {
  [leftDataType.TYPE]
}




function evaluateUserRole (lValue, rValue) {
  return lValue.id === rValue.id
}

function evaluateUserUserList (lValue, rValue) {
  return lValue.id === rValue.id
}
